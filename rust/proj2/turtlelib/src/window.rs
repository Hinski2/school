pub struct Window {
    x_size: usize, 
    y_size: usize, 
}

// window utils
impl Window {
    pub fn new() -> Window {
        todo();
    }
}

// window control
impl Window {
    pub fn clean() {
        todo!();
    }

    pub fn clear_screen() {
        todo!();
    }
}